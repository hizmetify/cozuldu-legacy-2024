import Button from '../UI/Button';

const Header = () => {
  return (
    <header>
      <Button backgroundColor="bg-white" color="text-gray-800" hoverColor="" />
    </header>
  );
};

export default Header;
